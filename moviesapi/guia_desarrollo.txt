1) Registrar la aplicación "nombre_app"  En el settings.py:

2) MOdificar el urls.py dentro de la carpeta del proyecto:

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
  path("admin/", admin.site.urls),
  path("", include("nombre_app.urls")),
]

3) Modificar urls.py de la app:

from django.urls import path
from Nombre_app.views import index

urlpatterns = [
    path('', index, name='index'),
]